services:
  server:
    build: .
    container_name: sismo-server
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - PORT=3001
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3